package com.nicecoc.data

import discord4j.rest.util.Color

/** Material theme red 500. */
val red: Color = Color.of(0xF44336)

/** Material theme pink 500. */
val pink: Color = Color.of(0xE91E63)

/** Material theme purple 500. */
val purple: Color = Color.of(0x9C27B0)

/** Material theme deep purple 500. */
val deepPurple: Color = Color.of(0x673AB7)

/** Material theme indigo 500. */
val indigo: Color = Color.of(0x3F51B5)

/** Material theme blue 500. */
val blue: Color = Color.of(0x2196F3)

/** Material theme light blue 500. */
val lightBlue: Color = Color.of(0x03A9F4)

/** Material theme cyan 500. */
val cyan: Color = Color.of(0x00BCD4)

/** Material theme teal 500. */
val teal: Color = Color.of(0x009688)

/** Material theme green 500. */
val green: Color = Color.of(0x4CAF50)

/** Material theme light green 500. */
val lightGreen: Color = Color.of(0x8BC34A)

/** Material theme lime 500. */
val lime: Color = Color.of(0xCDDC39)

/** Material theme yellow 500. */
val yellow: Color = Color.of(0xFFEB3B)

/** Material theme amber 500. */
val amber: Color = Color.of(0xFFC107)

/** Material theme orange 500. */
val orange: Color = Color.of(0xFF9800)

/** Material theme deep orange 500. */
val deepOrange: Color = Color.of(0xFF5722)
